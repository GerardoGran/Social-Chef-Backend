const request = require("supertest");
const app = require("../app");

test('Create a new Account', async (done) => {
  newCook = {
    fname: "testName",
    lname: "testLname",
    username: "testUser",
    email: "test@mail.com",
    password: "Pass.word1Test"
  };
});